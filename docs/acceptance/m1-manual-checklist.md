# M1 手动验收清单(骨架)

> 状态:**骨架,验收前由主线程展开**为逐条可执行步骤与判定标准。
> 定位:M1 验收包(`docs/acceptance/m1-acceptance.md`)引用的人工走查附页。
> 自动化覆盖:`scripts/regression-pi.sh`(pi 升级四项冒烟)、`scripts/isolation-check.sh`(双向隔离自动化部分);本清单只列脚本无权限/无法覆盖的人工项。

## 【环境准备】

| # | 动作 | 预期 | 勾选 |
|---|------|------|------|
| E1 | 以 harry 启动(WSL2),确认非 root | `whoami` = harry | [ ] |
| E2 | 确认隔离 env 变量生效(未污染全局) | `~/.pi` 无本轮 pidesk 会话文件 | [ ] |
| E3 | 预置一个全局 skill / 全局 command(用于 A3 渗透验证) | 全局 `~/.pi/skills` 存在测试 skill | [ ] |
| E4 | 本机 Ollama 可达且至少一个模型 | `curl localhost:11434/api/tags` 有模型 | [ ] |

## 【双向隔离(A3 手动补验步骤)】

> A1/A2/A4 已由 `scripts/isolation-check.sh` 自动覆盖;以下为脚本无预置权限的 A3 项,以及人工复核补充。

| # | 动作 | 预期 | 勾选 |
|---|------|------|------|
| A3-1 | 按 E3 预置全局 skill 后跑一轮 pidesk 对话 | 子进程 `PI_CODING_AGENT_DIR` 指向隔离 dataDir | [ ] |
| A3-2 | 在会话 `/` 或 command 列表检查全局 skill 是否出现 | 全局 skill **不渗透**进隔离会话 | [ ] |
| A3-3 | 对比 `~/.pi` 顶层与一级条目 mtime(对话前后) | 全局 `~/.pi` 零写入 | [ ] |
| A3-4 | 检查隔离 `sessions/` 出现本轮会话 JSONL | 会话落在隔离 dataDir 内 | [ ] |

## 【单会话对话走查】

| # | 动作 | 预期 | 勾选 |
|---|------|------|------|
| S1 | 新建对话,输入 `回复 ok` | 消息流式追加,结束回到空闲 | [ ] |
| S2 | 观察流式渲染 | `text_delta` 增量渲染;思考块折叠;无卡死 | [ ] |
| S3 | 中断当前长任务(点停止/触发 abort) | 停止生效,无继续输出,UI 状态回空闲 | [ ] |
| S4 | 关闭再打开会话(换进程恢复) | 会话历史完整回放,可继续对话 | [ ] |
| S5 | 首次使用视角:不看文档能否完成新建对话→对话→中断 | 核心操作 ≤3 步、报错人话(P5 红线) | [ ] |

## 【三会话并发】

| # | 动作 | 预期 | 勾选 |
|---|------|------|------|
| C1 | 同时开 3 个会话并发对话 | 三个 pi 子进程独立、互不串台 | [ ] |
| C2 | 检查三者会话文件落点 | 各自 JSONL 独立、无交叉写入 | [ ] |
| C3 | 对其中一个触发 abort/切换会话 | 仅影响该会话,其余不受扰 | [ ] |
| C4 | 并发下全局 `~/.pi` 快照对比 | 仍零写入 | [ ] |

## 【Windows 实测项(占位)】

> 占位:待 Windows 侧经 `\\wsl.localhost\Ubuntu\home\harry\pidesk` 访问或独立构建后展开。**验收前由主线程展开**。

| # | 动作 | 预期 | 勾选 |
|---|------|------|------|
| W1 | (占位)Windows 下启动 pidesk | 待展开 | [ ] |
| W2 | (占位)Windows↔WSL2 路径与文件访问 | 待展开 | [ ] |
| W3 | (占位)信号/退出兜底(DR-002)在 Windows 行为 | 待展开 | [ ] |
